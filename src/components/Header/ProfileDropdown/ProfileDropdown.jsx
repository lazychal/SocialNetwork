import React from 'react';
import s from './ProfileDropdown.module.scss'
import {NavLink} from "react-router-dom";

const ProfileDropdown = (props) => {
    return <div className={s.profileDropdown}>

    </div>
};

export default ProfileDropdown;